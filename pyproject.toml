[project]
name = "ultisnips"
version = "0.1.0"
description = "The ultimate snippet solution for Vim."
requires-python = ">=3.10"
dependencies = [
    "unidecode",
]

[dependency-groups]
dev = [
    "black",
    "mypy",
    "pylint",
]
